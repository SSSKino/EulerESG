PyYAML>=6.0
rich>=13.0

# Optional local GPU OCR dependency:
#   Install paddlepaddle-gpu manually according to your CUDA version,
#   then install: paddleocr[doc-parser]
# This project intentionally does not install CPU Paddle by default.
